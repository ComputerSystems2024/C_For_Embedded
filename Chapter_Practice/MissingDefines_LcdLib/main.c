
/**LCD Library to write data to lcd (modify the values of bit 10-3 and clear
 *  the R/W bit - set to 0 the bit 1
 * Reset the LCD(set RS bit -put 1 in the bit 0 of the registry)
 *   
 */

 
#include <stdio.h>
#include <inttypes.h>


int main() {
  printf("In main\n");

  return 0;
}
