
/************************************************************************************
 *  A function to count the vowels and consonats from a given character array
 * and writes the counts into an uint8_array given as a pointer
 * **********************************************************************************
 * 0.Declare an array of characters
 * 1.count vowels
 * 2.count consonants
 * 3.write the counts into an uint8_t array given as a pointer / uint8_t *counts
 *  */
// Includes
#include <stdint.h>
#include <stdio.h>

// function declarations
//  vowels and consonats counter
void charcounter(char *str, uint8_t *result);

int main() {
  printf("In main\n");

  return 0;
}

// function definitions
void charcounter(char *str, uint8_t *result) {}